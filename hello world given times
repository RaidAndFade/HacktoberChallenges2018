#include "stdio.h"

int main(void)
{
    int a;

    printf("Please input an integer value: ");
    scanf("%d", &a);
    
    
    for(int i=0; i<a; i++){
      printf("You entered: %d\n", a);
    }
      
    return 0;
}
